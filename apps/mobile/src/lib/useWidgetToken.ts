/**
 * DISABLED for v0.1.0 App Store submission — companion to the disabled
 * widget target and useHealthSync stub. Returns when the widget target
 * is re-enabled (see comment in useHealthSync.ts).
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function useWidgetToken(_userId: string | undefined) {
  // no-op
}
