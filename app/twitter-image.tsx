// Reuse the OG image for twitter:image so summary_large_image renders
// the same full-width card in X previews as it does in WhatsApp / iMessage.
export { default } from "./opengraph-image";
export { alt, size, contentType, runtime } from "./opengraph-image";
